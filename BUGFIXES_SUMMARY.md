# v1.3.1 (2026-07-07)

## Bug Fixes

- **Default config template**: Removed deprecated `upstream_dns` and `protocol` from auto‑generated `dosev.conf`. Now uses the new `[upstreams]` section with `ip` field and full comments.

## Other

- All tests passing (78/78).
